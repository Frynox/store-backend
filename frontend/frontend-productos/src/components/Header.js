import React from "react";

const Header = () => {
  return (
    <header className="header">
      <h1 className="brand-title">store-backend</h1>
      <nav className="nav-bar">
      </nav>
    </header>
  );
};

export default Header;